name: library_name
description: A reusable Flutter library skeleton.
version: 0.1.6
repository: https://github.com/{organization}/{repository}
homepage: https://github.com/{organization}/{repository}

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

