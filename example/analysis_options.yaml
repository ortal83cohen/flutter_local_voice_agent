include: package:flutter_lints/flutter.yaml

# example/lib web-conditional storage and preparation are analyzed with the
# rest of the example. The exclude list drops generated host trees only.

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
    - .dart_tool/**
