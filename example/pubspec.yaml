name: flutter_local_voice_agent_example
description: Foreground offline speech example with locally supplied model assets.
publish_to: none
version: 0.1.0+1
environment:
  sdk: ^3.13.0
dependencies:
  flutter:
    sdk: flutter
  flutter_local_voice_agent:
    path: ..
dev_dependencies:
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
